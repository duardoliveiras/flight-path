#include "Menu.h"
#include <stdio.h>
#include "Airport.h"
#include <iostream>

int main()
{
    // printf("Hello!\n");
    // Airport airport("code", "name", "country", "city", 0.0, 0.0);
    // std::cout << airport.getCity() << std::endl;
    Menu();
    return 0;
}